import test from 'node:test';import assert from 'node:assert/strict';import http from 'http';import {dispatch} from '../src/providers/knowledgeProviders.js';
test('SCM adapter requires payload-bound receipt',async()=>{const server=http.createServer((req,res)=>{res.setHeader('content-type','application/json');res.end(JSON.stringify({providerRequestId:'g1',payloadHash:req.headers['x-payload-hash']}));});await new Promise(r=>server.listen(0,'127.0.0.1',r));process.env.GITHUB_API_URL=`http://127.0.0.1:${server.address().port}`;process.env.GITHUB_TOKEN='test';try{assert.equal((await dispatch({provider:'github',operation:'fetch',payload:{},payload_hash:'a'.repeat(64),idempotency_key:'i'})).providerRequestId,'g1');}finally{server.close();}});
test('SCM adapter exposes failure for retry policy',async()=>{const server=http.createServer((_req,res)=>{res.statusCode=500;res.end('{}');});await new Promise(r=>server.listen(0,'127.0.0.1',r));process.env.GITHUB_API_URL=`http://127.0.0.1:${server.address().port}`;try{await assert.rejects(()=>dispatch({provider:'github',operation:'fetch',payload:{},payload_hash:'b'.repeat(64),idempotency_key:'i2'}),/provider_500/);}finally{server.close();}});
